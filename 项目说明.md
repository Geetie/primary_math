# 小学数学应用题自动解题 - 项目说明

## 项目概述

本项目是复旦大学《人工智能-LLM实践》课程的作业实现，针对小学数学1-6年级应用题自动解题任务，实现了多个解决方案。

## 快速开始

### 1. 环境准备

```bash
# 安装依赖
pip install -r requirements.txt
```

### 2. 运行测试

```bash
# 测试代码和依赖
python test_code.py
```

### 3. 运行方案

#### Baseline方案
```bash
# Windows
run_baseline.bat

# 或手动
python baseline/train.py
python baseline/inference.py
```

#### 方案1：COT优化
```bash
# Windows
run_scheme1.bat

# 或手动
python scheme1_cot/inference.py
```

#### 方案2：数据构建与增强
```bash
# Windows
run_scheme2.bat

# 或手动
python scheme2_data_enhancement/generate_cot_data.py
python scheme2_data_enhancement/train_with_cot.py
python scheme2_data_enhancement/inference.py
```

## 项目结构

```
primary_math/
├── data/                       # 数据集
│   ├── train.json             # 训练数据（12000条）
│   ├── test.json              # 测试数据（8000条）
│   └── submit.csv             # 提交模板
├── models/                     # 模型缓存目录
├── outputs/                    # 输出目录
│   ├── baseline/              # Baseline模型
│   ├── scheme1_cot/           # 方案1结果
│   └── scheme2_cot/           # 方案2模型
├── utils/                      # 工具模块
│   ├── common.py              # 通用工具函数
│   └── model_loader.py        # 模型加载工具
├── baseline/                   # Baseline方案
│   ├── train.py               # 训练脚本
│   └── inference.py           # 推理脚本
├── scheme1_cot/               # 方案1: COT优化
│   ├── cot_prompts.py         # COT提示模板
│   └── inference.py           # 推理脚本
├── scheme2_data_enhancement/  # 方案2: 数据构建与增强
│   ├── generate_cot_data.py   # 生成COT数据
│   ├── train_with_cot.py      # 使用COT数据训练
│   └── inference.py           # 推理脚本
├── report/                    # 实验报告
│   └── 实验报告模板.md         # 报告模板
├── run_baseline.bat           # 运行Baseline
├── run_scheme1.bat            # 运行方案1
├── run_scheme2.bat            # 运行方案2
├── test_code.py               # 代码测试
├── upload_to_kaggle.py        # Kaggle上传工具
├── requirements.txt           # 依赖列表
├── README.md                  # 项目说明
└── 项目说明.md                 # 本文档
```

## 方案详解

### Baseline方案

**核心思想**：使用LoRA微调Qwen2.5-0.5B

**实现细节**：
- 基础模型：Qwen/Qwen2.5-0.5B-Instruct
- 微调方法：LoRA（r=8, alpha=32）
- 训练数据：原始12,000条训练数据
- 训练配置：5 epochs, lr=1e-4, batch_size=4

**特点**：
- 支持断点续训
- 自动下载模型
- CPU/GPU兼容

### 方案1：COT优化

**核心思想**：通过提示工程激活模型推理能力

**实现细节**：
- Standard：标准提示
- Zero-shot COT：零样本思维链
- Few-shot COT：提供示例引导

**特点**：
- 无需训练
- 快速对比不同提示效果
- 可与其他方案结合

### 方案2：数据构建与增强

**核心思想**：改进训练数据质量

**实现细节**：
- COT数据生成：为训练数据补充解题步骤
- 数据增强：通过数字替换合成新样本
- 使用带COT的数据进行SFT微调

**特点**：
- 提升数据质量
- 增加数据多样性
- 需要额外训练时间

## 关键技术点

### 1. 断点续训

所有训练脚本支持断点续训：
```python
training_args = TrainingArguments(
    resume_from_checkpoint=True if os.path.exists(output_dir) else None,
)
trainer.train(resume_from_checkpoint=training_args.resume_from_checkpoint)
```

### 2. 自动下载检测

模型下载支持检测已下载文件，避免重复下载：
```python
def check_model_downloaded(model_path: str) -> bool:
    return os.path.exists(model_path) and os.path.isdir(model_path)
```

### 3. 数字答案提取

智能提取模型输出中的数字答案：
```python
def extract_number(text: str) -> str:
    patterns = [r'-?\d+\.\d+', r'-?\d+']
    for pattern in patterns:
        matches = re.findall(pattern, text)
        if matches:
            return matches[-1]  # 返回最后一个匹配
    return ""
```

### 4. CPU/GPU兼容

代码自动检测可用设备：
```python
def get_device() -> str:
    if torch.cuda.is_available():
        return "cuda"
    else:
        return "cpu"
```

## Kaggle使用指南

### 方法一：使用上传脚本

```bash
python upload_to_kaggle.py
```

选择选项创建上传包或Notebook。

### 方法二：手动上传

1. 访问 https://www.kaggle.com/datasets
2. 创建新数据集
3. 上传代码和数据文件
4. 创建Notebook并运行

## 提交文件

所有方案的提交文件保存在 `outputs/` 目录：

- `baseline_submit.csv` - Baseline结果
- `scheme1_submit_*.csv` - 方案1不同提示的结果
- `scheme2_submit.csv` - 方案2结果

提交格式：
```csv
id,ret
0,85
1,64
...
```

## 评分计算

**个人完成**：
- 比赛结果分：`s1 = 正确率 * 15`
- 工作量分：`s2 = 完成方案数 * 5`
- 总分：`s = min(s1 + s2, 15)`

**示例**：
- 正确率0.3 + 完成2个方案 = 0.3*15 + 2*5 = 4.5 + 10 = 14.5分

## 注意事项

1. **模型限制**：仅使用0.5B及以下参数模型
2. **数据约束**：严禁处理测试数据
3. **提交限制**：每日每个账号可提交3次
4. **截止时间**：
   - 比赛提交：2026年06月11日
   - 报告提交：2026年06月19日

## 常见问题

### Q1: 模型下载失败怎么办？

A: 检查网络连接，或手动下载模型到 `models/` 目录。

### Q2: CUDA内存不足怎么办？

A: 减小batch_size或gradient_accumulation_steps。

### Q3: 如何在CPU上运行？

A: 代码自动检测设备，CPU上会自动调整配置。

### Q4: 如何查看训练进度？

A: 训练日志会显示在控制台，包含loss和学习率信息。

## 联系与支持

如有问题，请参考：
- 课程文档
- 比赛官网：https://www.datafountain.cn/competitions/467
- 官方Baseline：https://github.com/AI-FDU/Math_Solver

## 更新日志

### v1.0 (2026-05-25)
- 初始版本
- 实现Baseline、方案1、方案2
- 添加完整的文档和测试

---

**最后更新**：2026年05月25日
