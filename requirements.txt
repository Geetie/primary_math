# 基础依赖
torch>=2.0.0
transformers>=4.38.0
peft>=0.7.0
accelerate>=0.25.0

# 训练框架
trl>=0.12.0
datasets>=2.14.0

# 模型下载
modelscope>=1.10.0

# 数据处理
tqdm>=4.65.0
numpy>=1.24.0
requests>=2.28.0

# 其他
sentencepiece>=0.1.99
protobuf>=3.20.0

# GPU加速（A10 Ampere架构支持，需CUDA编译环境）
# 安装命令: pip install flash-attn --no-build-isolation
# flash-attn>=2.0.0
